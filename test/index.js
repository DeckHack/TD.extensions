const { expect } = require('chai');
const TDE = require('../dist');

describe('TDE', () => {
	it('Initialize TDE', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Add Extension', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Remove Extension', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Get Extension', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Get All Extensions', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Get All Enabeld Extensions', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Get All Initialized Extensions', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Enable Extension', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Disable Extension', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Initialize', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Check Dependencies', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Check For Conflicts', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Is Extension Enabled', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Is Extension Initialized', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Resolve Dependency Graph', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Initialize Extension', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Destroy Extension', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
	it('Re-order Enabled Extensions', () => {
		// const result = new TDE();
		// expect(result).to.equal('Hello world!');
		return true;
	});
});